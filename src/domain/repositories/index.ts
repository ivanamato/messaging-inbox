/**
 * Domain repository interfaces.
 * Re-exports all repository types for convenient imports.
 */

export type {
  ChatRepository,
  MessageRepository,
  MediaRepository,
  ConnectionRepository,
  RepositorySet,
} from './types';
